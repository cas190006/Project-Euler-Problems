import math
"""
The fraction 49/98 is a curious fraction, as an inexperienced mathematician in attempting to simplify it may incorrectly believe that 49/98 = 4/8, which is correct, is obtained by cancelling the 9s.

We shall consider fractions like, 30/50 = 3/5, to be trivial examples.

There are exactly four non-trivial examples of this type of fraction, less than one in value, and containing two digits in the numerator and denominator.

If the product of these four fractions is given in its lowest common terms, find the value of the denominator.

ANSWER: 100
"""

# Time: O(1),  Space: O(1)
def digitCancellingFractionsProduct():
    numeratorProduct = 1
    denominatorProduct = 1
    
    for numerator in range(11, 99):
        if numerator % 10 == 0:
            continue
        
        ones = numerator % 10
        
        for last_digit in range(1, 10):
            denominator = ones * 10 + last_digit
            
            if denominator <= numerator:
                break
                
            if numerator * (denominator % 10) == denominator * (numerator // 10):
                numeratorProduct *= numerator
                denominatorProduct *= denominator
                        
    return denominatorProduct // math.gcd(numeratorProduct, denominatorProduct)


print(digitCancellingFractionsProduct())
